function start(){
    const canvas = document.querySelector('#canvas');
    const ctx = canvas.getContext('2d')
    let w, h;
    function init(){
        w = canvas.width;
        h = canvas.height;
    }
    init()
}